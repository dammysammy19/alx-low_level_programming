#include "main.h"

/**
 * _islower - checks for lowercsse letters
 *
 * Return: Always 0 (Success)
 */

int _islower(int c)
{
	if (x >= 'a' && x <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
